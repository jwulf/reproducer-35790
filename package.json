{
  "name": "reproducer-35790",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Run 'npm run test:local' or 'npm run test:saas' to execute tests\nSet credentials in camunda-test-config-saas.json for the SaaS test.\n\"",
    "test:local": "counter=1; while npx jest test.spec; do echo \"Test passed (run $counter), running again...\"; ((counter++)); done; echo \"Test failed after $((counter-1)) successful runs\"",
    "test:saas": "counter=1; while CAMUNDA_TEST_CONFIG_FILE=camunda-test-config-saas.json npx jest test.spec; do echo \"Test passed (run $counter), running again...\"; ((counter++)); done; echo \"Test failed after $((counter-1)) successful runs\"",
    "test:saas:poll": "counter=1; while CAMUNDA_TEST_CONFIG_FILE=camunda-test-config-saas.json npx jest test-with-poll; do echo \"Test passed (run $counter), running again...\"; ((counter++)); done; echo \"Test failed after $((counter-1)) successful runs\"",
    "test:collector": "ts-node failure.collector.ts"
  },
  "keywords": [],
  "author": "Josh Wulf <josh.wulf@camunda.com>",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "@camunda8/sdk": "^8.7.24"
  },
  "devDependencies": {
    "@camunda8/process-test": "^1.7.0",
    "@types/jest": "^30.0.0",
    "jest": "^30.0.5",
    "ts-jest": "^29.4.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
